using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItGameManager : MonoBehaviour
{
    public Dictionary<int, bool> it_Dict;
    // Start is called before the first frame update
    void Start()
    {
        it_Dict =  new Dictionary<int, bool>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
